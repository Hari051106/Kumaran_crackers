<?xml version="1.0" encoding="utf-8"?>
<!--
  Plain HTTP is allowed only for the addresses used during local development:
  10.0.2.2 is the host machine as seen from the Android emulator.

  Everything else must be HTTPS, so a release build cannot accidentally send
  a customer's credentials or order over an unencrypted connection.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
</network-security-config>
